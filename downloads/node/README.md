# Node fallback download

`codex-kimi-bridge-node-0.1.0.tgz` installs the fallback command `codex-kimi-bridge-node` and requires Node.js 20 or later.

The default download is the Rust `codex-kimi-bridge` macOS kit in the parent directory. Do not run both implementations on port 8787.
