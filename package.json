{
  "name": "codex-kimi-bridge",
  "version": "0.1.0",
  "description": "A zero-dependency local bridge from the OpenAI Responses API shape used by Codex to Kimi Code Chat Completions.",
  "type": "module",
  "bin": {
    "codex-kimi-bridge": "./bin/codex-kimi-bridge.mjs"
  },
  "files": [
    "bin/",
    "src/",
    "fixtures/",
    "test/",
    "companion-skill/",
    "install/",
    "README.md",
    "PROVENANCE.md",
    "SECURITY.md",
    "LICENSE",
    "start-codex-kimi-bridge.command"
  ],
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/codex-kimi-bridge.mjs && node --check src/cli.mjs && node --check src/server.mjs && node --check src/protocol.mjs && node --check src/reasoning-store.mjs && node --check src/sse.mjs",
    "smoke": "node bin/codex-kimi-bridge.mjs --help"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "keywords": [
    "codex",
    "kimi",
    "responses-api",
    "bridge"
  ]
}
